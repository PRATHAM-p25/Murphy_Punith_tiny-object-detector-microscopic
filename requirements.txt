# Streamlit app and inference deps
streamlit==1.51.0

# Ultralytics wrapper (loads ONNX exported by Ultralytics)
ultralytics==8.3.229

# ONNX runtime — use a newer available build (1.23.x is widely available)
onnxruntime==1.23.2

# OpenCV without GUI (works on servers)
opencv-python-headless==4.8.0.74

# Image & array libs
Pillow>=9.0.0
numpy>=1.24.0

# HTTP + DB
requests>=2.30.0
pymongo==4.5.1
dnspython==2.4.2

# Optional utilities (small)
python-dotenv>=1.0.0
